[build]
  command = "node scripts/build-with-image-rules.mjs && node scripts/inject-floating-whatsapp.mjs"
  publish = "deploy-inline"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
